components:
  schemas:
    ObjectB:
      properties:
        name:
          type: string
        nested:
          $ref: "#/components/schemas/ObjectC"
    ObjectC:
      properties:
        name:
          type: string
